# 内部自动化提示词

这个目录属于重置雷达的内部运营层，不是网站页面内容。

- `side-hustle-curator.md`：候选搜索、去重、脱敏、入库与自检。
- `side-hustle-reviewer.md`：独立审查内容真实性、泄漏风险和发布条件。

两个文件都不得被导入 `website/app`、`website/components`或复制到 `website/public`。它们只能由未来的后台任务在私有仓库中读取。

现在仅存放规则，不代表自动任务已启用，也不会自动推送 GitHub。
